[test]
preload = ["./src/preload.ts"]
# Consumer sandbox has its own package.json / bunfig; run via test/integration spawn.
pathIgnorePatterns = ["test/real-world/**"]

# Coverage: `bun test --coverage` (see package.json).
