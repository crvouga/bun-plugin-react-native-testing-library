[test]
preload = ["./src/preload.ts"]
# Consumer sandboxes have their own package.json / bunfig; run via integration spawn.
pathIgnorePatterns = ["test/real-world/**", "test/real-world-expo/**"]

# Coverage: `bun test --coverage` (see package.json).
